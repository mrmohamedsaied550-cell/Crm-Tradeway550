{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile=false && cd apps/api && pnpm db:generate && pnpm build && cd ../web && pnpm build"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm db:migrate && pnpm db:seed && pnpm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
