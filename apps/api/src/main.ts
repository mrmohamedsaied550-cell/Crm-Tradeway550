/**
 * @crm/api — placeholder entrypoint.
 *
 * C1 ships an empty scaffold. The real Nest bootstrap (HTTP server, validation
 * pipe, OpenAPI, request-id middleware, /health endpoint) lands in C3.
 */
function main(): void {
  // eslint-disable-next-line no-console
  console.log('hello from @crm/api');
}

main();
